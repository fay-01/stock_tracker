# 📈 股票交易记录系统

一个简洁、实用的股票交易记录和管理系统，支持多设备同步访问。

## ✨ 功能特性

- 🔐 **用户认证**：注册/登录系统，数据隔离
- 💼 **交易记录**：记录每笔买卖操作，包括股票代码、价格、数量和想法
- 📝 **每日复盘**：记录投资心得和当日盈亏
- 📊 **报表统计**：支持日/月/年报表，清晰展示交易数据
- 📱 **响应式设计**：完美适配电脑、手机、平板

## 🚀 快速部署

### 部署到 Render（免费，推荐）

1. **上传代码到 GitHub**
   ```bash
   cd stock-tracker
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <你的 GitHub 仓库地址>
   git push -u origin main
   ```

2. **在 Render 上创建服务**
   - 访问 [render.com](https://render.com)
   - 使用 GitHub 账号登录
   - 点击 "New +" → "Web Service"
   - 选择你的仓库
   - 使用以下配置：
     - Build Command: `pip install -r requirements.txt`
     - Start Command: `gunicorn app:app`
     - Plan: Free

3. **获取访问地址**
   - 部署完成后，你会获得 `https://xxx.onrender.com` 的地址
   - 在任何设备上用浏览器访问即可

### 部署到 Railway

1. 访问 [railway.app](https://railway.app)
2. 使用 GitHub 账号登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择你的仓库，自动部署

## 💻 本地开发

### 环境要求

- Python 3.8+
- pip

### 安装运行

```bash
# 安装依赖
pip install -r requirements.txt

# 运行应用
python app.py

# 访问 http://localhost:5000
```

## 📁 项目结构

```
stock-tracker/
├── app.py                 # Flask 应用主文件
├── requirements.txt       # Python 依赖
├── render.yaml           # Render 部署配置
├── Procfile             # 进程文件
├── templates/           # HTML 模板
│   ├── base.html
│   ├── index.html
│   ├── login.html
│   ├── register.html
│   ├── dashboard.html
│   ├── trades.html
│   ├── reflections.html
│   ├── reports.html
│   └── error.html
└── static/
    └── css/
        └── style.css    # 样式文件
```

## 🎯 使用说明

1. **注册账号**：访问网站首页，点击"免费注册"
2. **登录系统**：使用注册的账号密码登录
3. **添加交易**：在"交易记录"页面添加买卖操作
4. **写复盘**：在"每日复盘"页面记录投资心得
5. **查看报表**：在"报表统计"查看日/月/年统计

## 🔒 数据安全

- 密码使用 bcrypt 加密存储
- 每个用户的数据完全隔离
- 建议使用强密码

## 📝 技术栈

- **后端**：Flask + SQLAlchemy
- **数据库**：SQLite（本地）/ PostgreSQL（云端）
- **前端**：HTML5 + CSS3
- **部署**：Render / Railway

## 🆓 免费额度说明

### Render Free Plan
- 每月 750 小时免费额度（足够 24/7 运行）
- 免费 PostgreSQL 数据库（90 天）
- 适合个人使用

### Railway Free Plan
- 每月 $5 免费额度
- 按使用量计费
- 个人使用基本够用

## 📞 支持

如有问题，请查看：
- [DEPLOY.md](DEPLOY.md) - 详细部署指南

---

**让投资更有迹可循！** 📈
